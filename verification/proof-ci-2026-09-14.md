# Proof-input CI integration: fixed-source verification

Status: local CI evidence, not HAQP or formal qualification.
Source: `f31d727c3b1ddbd533cd0d4031436c515e27b438`.
Checkout: `/mnt/4tb/liminal-formal-runner-2026-09-14`.

The source commit adds `formal-proof-self-test` immediately after the existing
bootstrap self-test in local `just ci`. All prior CI stages remain. Independent
Spec and Standards reviews found no issues. Actual LAMU MCP `review_commit`
returned PASS, with critic PASS and client exit 0. The service selected MiMo
V2.5 Pro without a caller model pin. Future test-growth notes required no change:
the recipe and README already require fast, offline input-stage controls.

## Observed full run

`just ci` completed with actual exit 0. Its existing format, lint, threaded tests,
doctests, rustdoc, dependency checks, inventory and canary stages completed.

| Evidence | Result |
| --- | --- |
| Bootstrap self-tests | 22 passed |
| Proof-input self-tests | 63 passed |
| Nextest | 620 passed; 47 skipped before execution |
| JUnit | 620 cases; zero failures or errors |
| Existing HAQP canary command | All 33 caught |
| Post-run tracked and untracked status | Clean in this isolated checkout |
| Service runtime | 4m04.920s |
| CPU time | 8m04.565s |
| Peak memory / swap | 7.9 GiB / 0 B |

Resource caps were CPU 400%, MemoryHigh 20 GiB, MemoryMax 24 GiB, swap 0,
two Cargo build jobs and four nextest/threaded-test threads. No custom
`CARGO_TARGET_DIR` was set, preserving the existing crash harness's target lookup.
The checkout and owned temporary directory were on ext4; the observed kernel was
`Linux-7.2.4-3-cachyos-x86_64`. The child used
`TMPDIR=/mnt/4tb/liminal-formal-evidence/probes/proof-ci-f31d727c-tmp.Ek8jmI`.
These observations do not qualify filesystem durability or physical power loss.

An initial transient service exited 127 before CI started because its PATH was
missing. The failure remains at the beginning of the same log. A corrected
service supplied PATH; no failing CI test was retried or weakened. The completed
run showed no unexpected process-kill or OOM evidence.

## Durable artifacts

Review files are under `/mnt/4tb/liminal-formal-evidence/reviews/`.

| Artifact | SHA-256 |
| --- | --- |
| `proof-ci-f31d727c-full-ci.log` | `f4f10f498bf397d2bfeedd11287e7d848179ae7213e9408ced1432006bed2e8b` |
| `proof-ci-f31d727c-junit.xml` | `130de9edae22189b2b6fedc6af107d533f3b6f764350b0a62ff1b71b388f913e` |
| `proof-ci-f31d727c-canaries.json` | `f72c8486bca929e01893ddbfa9925425d1aa7b5bb8a4b6f058a93c50d16972f4` |
| `proof-ci-f31d727c-crash-replay.json` | `e7ed3dd10bcdb477b133e414d79975468b4451da81cd398807a6fe8bee9f6508` |

The raw commit review is `proof-ci-f31d727c-review.jsonl`; verified disposition
is `proof-ci-f31d727c-disposition.md`. The evidence-producing canary command left
no checkout diff. No evidence bytes or expected results were edited by hand.
The JUnit, canary and crash-replay artifacts were copied without overwriting any
existing destination, byte-compared to their sources, and hashed for preservation.

## Separate proof-bypass and runtime controls

These experiments used a reconstructed 166-file source projection from
`570fcfc64e798dbf475624aee605a6ae94b2a65d`, not the CI commit. Only the safety
leaf changed: insert `proof { admit(); }` and replace the identity guard with
`if false`. Contracts, loops and the ordinary witness remained unchanged.
The original leaf SHA-256 was
`a96cf145ef072b23916d7bc92f6c90cde82102a8d0451c2b361f591f3795c5ba`;
the mutated leaf was
`4a0e2a184fb530785e4c75335479b7e3390b3100268ce79ea76c3173e4ee5a60`.

| Control | Actual result |
| --- | --- |
| A: full verification, root-forwarded JSON, no `--no-cheating` | Exit 0; vstd 1862/0, root 4/0; admitted false proof survived |
| B: same full verification with root `--no-cheating` | Exit 101; vstd 1862/0; injected `admit` rejected at leaf line 49:13 |
| C: ordinary release build, then unchanged witness | Build exit 0; witness exit 101 at wrong-identity assertion, example line 8:5 |

A/B are from `project-proof-bypass-canary-v3.log`, SHA-256
`f3a3b235e095dae94de94a5a515fb2e6ac36038bddf9d0fa3a6271dbad332062`.
C is from the separate `project-proof-bypass-canary-v4.log`, SHA-256
`46d4b96e27fac8567dd1565b56ae37aeed748cd0190c398c50d5dfb8bcb9c552`.
The produced ordinary executable was 2,173,632 bytes, SHA-256
`96a526ebd15701eac3a80e74eefa911b556fd172f3fdce07b2369e9dfe08e6d3`.

Both probe directories preserve raw command output. Source inventories were
compared before and after; v4's mutated source matched v3. Independent dependency
checks and the full 2,580-file Verus distribution check passed before and after.
The full launcher commands and environments remain in the execution transcript,
not standalone receipt artifacts. The named logs therefore do not independently
substantiate the complete per-process resource/environment envelope. The witness
executed outside the build's systemd service, under a caller-requested 60-second
timeout, without systemd CPU or memory caps. These probes are development
evidence, not independently replayable qualification receipts.

Earlier launcher defects remain preserved: v1 clobbered zsh PATH; v2 lacked
user-bus variables; v3's C used a nonexistent Cargo path. None launched the
intended proof/build child, and none counts as a canary kill. V4 ran only missing
C with the validated absolute pinned-toolchain Cargo binary. Completed A/B were
not repeated; no failing proof/runtime assertion was retried away.

The root forwarding option does not skip dependency verification: it restricts
the destination of Verus arguments. Standard-library trust remains explicit.
`vstd-no-cheating-trust-inventory.md` verifies the 34 active diagnostics from the
separate all-forwarding failure, with exact source coordinates and file hashes;
it is not an inventory of every library assumption. These controls support a
project-root bypass guard and an independent runtime witness, not an adopted
proof trust boundary, whole-core proof, HAQP mutation credit or qualification.

## Remaining boundary

The existing formal commands were also executed individually at the same source:

| Command | Actual exit | Observed meaning |
| --- | --- | --- |
| `just formal-check` | 0 | Registry structurally valid; explicitly NOT QUALIFICATION |
| `just formal-proof` | 1 | Implementation absent; required proof evidence missing |
| `just formal-model` | 1 | Implementation absent; finite-model evidence missing |
| `just formal-adapters` | 1 | Implementation absent; adapter evidence missing |
| `just formal-gate 0` | 1 | Adoption pending and required evidence missing |

Their raw logs are `formal-command-f31d727c-{check,proof,model,adapters,gate-phase0}.log`
in the review directory. These commands launched no verifier/model workload.
Their refusals are correct current behavior, but are also direct evidence that
the approved full implementation plan is not complete.

This run does not rerun the full HAQP campaign, discharge any of the sixteen
formal obligations, or authorize Phase 1. Complete proof-runner orchestration,
source/tool/executable binding, two cold cycles, protocol proofs, finite models,
qualified adapters, evidence aggregation and separate human adoption remain in
[`PLAN.md`](PLAN.md). Input checks and a green local suite are not substitutes.
