version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
  - package-ecosystem: cargo
    directory: /
    schedule:
      interval: weekly
    # Lockfile maintenance only; workspace [workspace.dependencies] bumps stay deliberate.
    versioning-strategy: lockfile-only
