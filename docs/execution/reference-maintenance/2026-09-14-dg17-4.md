# DG17.4 registry maintenance audit

Brian approved the conditional DG17.4 proposal on 2026-09-14. Approval was
recorded before registry edits in commit `6451c0c6`. This audit implements only
the exact dependency-name addition and the existing deferred epoch-site move.
It does not grant Phase 0 GO, ratify the suite, or extend crash coverage.

## Dependency source audit

The audited package is `same-file` 1.0.6, already pinned as `=1.0.6` in Cargo.
The concurrency registry is a name allow-list, not a version verifier. Its
closed-list algorithm, exclusions and negative controls are unchanged.

Both primary inspection and a separate MiMo V2.5 Pro source inventory examined
the complete cached manifest and all four source modules. There are no exported
macros, thread/process creation calls, async runtime, or manifest features.
The private test-only `err` macro and `doc_comment::doctest!` are not exported
runtime spawning macros. Unix `Handle::from_file` retains the file and reads
metadata; equality compares device and inode. Windows delegates to `winapi-util`;
this audit is not a transitive Windows dependency or platform qualification.
Documented Windows file-index false positives and unsupported-platform errors
remain limitations, as does point-in-time rather than future file identity.

Reviewer correction: `winapi-util` is target-specific, not `optional = true`;
its `0.1.1` manifest requirement is not an exact resolved-version assertion.
The reviewer did not receive authority to approve a registry change.

Package file SHA-256 values:

| File | SHA-256 |
|---|---|
| Cargo.toml | `991f8df8fa5a259801900a56908cf21a66c5cf7b238bc81ba9bdf348e233252e` |
| src/lib.rs | `b22c2f0b5cad2248f16f4f42add52b2dc0c627631f71ee67a8c38fe305048f85` |
| src/unix.rs | `69abed9fade151247696c6d4a442ef299554f3722e23a2d08053598a52a27d62` |
| src/win.rs | `94f912cc3734f60608d0ee2b0c664afb65fc96e5b0b223a53565fb8998c03fa3` |
| src/unknown.rs | `bfde4e9ac88f500c0ccb69165383682ddd24bf7d7ddaf5859426e1fd4b2f9359` |

The independent response is retained at
`/mnt/4tb/liminal-formal-evidence/reviews/dg17-4-source-review.json`, SHA-256
`1912f8e47c46596f68721ec751aa2392aa92359d77417a9dea9b2587f0bcd9c7`.
The earlier failed response remains separate and is not counted as review.

## Epoch transaction comparison

Baseline `64ee56528df5f95b0f3c12f7f881942a6b2343f3`:
`crates/liminal-daemon/src/workspace.rs:284`, `persist_epoch`, contains one
`begin`, one `put_aux(SYS_EPOCH, "epoch", Value::from(epoch.0))`, and one
`commit` at line 291. Source SHA-256:
`ed75446e4a80fdcbc30785bedb089fa95373196ee53c435a40b394c537e9ab2a`.

At approval commit `6451c0c6`, the same lazy caller delegates at
`crates/liminal-daemon/src/workspace.rs:310` to `EpochWriter::persist`.
`crates/liminal-graph/src/owner.rs:120` begins the transaction, line 121 writes
the same auxiliary key and value, and line 122 commits the supplied metadata.
Metadata remains actor `None`, origin `Human`, current timestamp, provenance
`epoch:persist`, and inverse `None`. The wrapper still propagates errors and
returns unit. Timestamp evaluation now precedes `begin`, whereas previously
it followed `put_aux`; this is not a claim of byte-identical timestamps or
identical internal evaluation order. The persisted operation and durable
transaction boundary remain the existing epoch write, not an ILRP operation.

Current source SHA-256 values:

| File | SHA-256 |
|---|---|
| crates/liminal-daemon/src/workspace.rs | `3a9525b63128097a9b8fd648f1fcf5e60b03f23841300e5555b7047338ed42da` |
| crates/liminal-graph/src/owner.rs | `c24c79726528489ff3dfc3c47fed92760f1c74eaa36e8b4634ce9da96d3362ea` |

The packet replaces exactly one deferred row's path, retaining `sites: 1` and
its deferred classification. It does not add a deferred site or claim coverage
of this write. The packet digest is recomputed by `haq packet-digest`, with its
review-document mirror updated. No constitutive golden changes are authorized.

## Verification boundary

The earlier five registry failures remain recorded in
`verification/ilrp-verification-2026-09-13.md`. This source audit is not a test
pass. Full fixed-candidate `just ci` must be rerun with the existing assertions,
zero retries, serialized crash group, native workspace target directory and
bounded compute resources. Results must identify the tested commit; new
candidate qualification and remaining StoreOwner admission work stay separate.
