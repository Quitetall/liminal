# F-80 store-lock fix: reference maintenance

Authority: AM-17.13 for exact reference maintenance. The production change is
F-80's store-lock fix, which Brian authorized on 2026-09-24 ("fix F-80, we need
it done"). This record is committed in the same change and embeds no
containing-commit hash. It authorizes no new mutant, changes no threshold,
establishes no kill, and does not qualify the changed candidate.

Old source commit: `fe65ccf9a6198d6bd07ab978d54df7ae53dfbba6`.

## What moved, and why

F-80 adds `impl Drop for Inner` beside `Inner`'s `Debug` impl, and rewrites the
rationale in `acquire_write_lock`'s doc comment. Both sit between lines 258 and
300 of `crates/liminal-graph/src/store/mod.rs`. A test module is appended at the
end of the file. The diff has exactly four hunks:

```
@@ -258,0 +259,17 @@ impl std::fmt::Debug for Inner {
@@ -275,6 +292,5 @@ const LOCK_ACQUIRE_BUDGET ...
@@ -282,5 +298,6 @@ const LOCK_ACQUIRE_BUDGET ...
@@ -869,0 +887,54 @@ impl GraphStore {
```

So every line after 300 moves down by exactly 17, and nothing inside any
function body changes. Six of the file's nine anchored mutants sit above line
258 and do not move: P1-M017, M018, M023, M024, M025 and M026. The three below
move by +17:

| Mutant | Operator | Requirement | Killing tests | Old | New | Enclosing symbol | Anchor expression |
|---|---|---|---|---|---|---|---|
| P1-M019 | success-error-substitution | P1-R011 | P1-T13, P1-T14 | 334 | 351 | `GraphStore::head` | `Ok(self.lock()?.state.head)` |
| P1-M021 | wrong-holder-selection | P1-R012 | P1-T15, P1-T16 | 440 | 457 | `GraphStore::node_at` | `return Ok(inner.state.nodes.get(&id).cloned());` |
| P1-M022 | skipped-durable-transition | P1-R015 | P1-T22, P1-T07 | 629 | 646 | `GraphStore::commit_txn` | `inner.log.append(&record)?;` |

All three remain in family `graph/interchange codecs` with disposition
`predeclared`. Their defect strings and every other packet field are
unchanged.

Each anchor expression occurs exactly once in both the old and the new file, so
no match was chosen by proximity. The enclosing symbol is the same function
before and after. Each line's bytes are unchanged, so each mutant applies the
same transformation to the same expression.

## Files

| File | SHA-256 |
|---|---|
| old `crates/liminal-graph/src/store/mod.rs` (at the old source commit) | `196548f10063f06e122f0c46ffaabeac0ee822b8156ec03e6cea52051c591e1c` |
| new `crates/liminal-graph/src/store/mod.rs` | `4cea95bbad53f3cb257db0b0fa74752d5143e1815cf9e6ad17320167576b7ee0` |

Changed references: the three `mutant_source_coordinate` line numbers in
`crates/liminal-xtask/src/haq.rs`, and the matching three packet `source`
fields. No registry file, anchor text, or ID changed.

## Packet

- Old packet digest: `433e08b49b6896e7d72ae97a1ec653ba31c1dbdfead88c5442cf956384f59c96`.
- New packet digest: `d8693e75535c2dd63cd82936bd950dcbaaff1f41b2c6f02ec9d2b625acb515b3`,
  from `cargo run -p liminal-xtask -- haq packet-digest` (exit 0).
- The mirror in `docs/execution/phase1-suite-review.md` must carry the new
  digest. See Verification for why that edit is not yet in this change.

## Independent review

No model review was run. Mandatory model reviews are suspended by owner
decision for all projects, and this record does not claim one. The target
evidence above can be re-checked from the two file hashes and the diff hunks
alone.

## Verification

- `cargo nextest run -p liminal-graph -p liminal-daemon`: 49 passed.
- The new regression test
  `store::tests::a_dropped_store_is_not_held_by_a_child_sharing_its_lock` fails
  with the unlock removed (at 0.253s, the acquisition budget) and passes with it.
- `cargo test --workspace --all-targets` (the `test-threaded` lane F-80 was found
  in): 611 passed. The 4 failures are all
  `review packet markdown missing packet digest d8693e75…`, the pending mirror
  edit above. `m05::overlay_aging_escalates` and
  `m08::freeze_rejects_stale_file_bytes` passed.
- `haq verify-inventory` fails for the same missing mirror, and cannot pass
  until it is applied. The automated permission check blocked the edit, so it
  waits for Brian.

Old evidence stays historical. Canary, crash and mutation evidence bound to
the old packet digest must be regenerated by their existing producers, not
edited.
