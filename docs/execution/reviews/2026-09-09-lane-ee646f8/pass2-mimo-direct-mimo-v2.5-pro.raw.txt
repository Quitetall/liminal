```json
{
  "attempts": [
    {
      "id": "A01",
      "attack_class": "vacuity",
      "target": "crates/liminal-xtask/src/haq.rs:142",
      "attempt": "falsify crates/liminal-xtask/src/haq.rs:142 by replacing the predicate in anchor_is_mutable_line that checks for declaration keywords with a constant true, so every line is treated as mutable and the mutant anchor screen becomes vacuous",
      "observed_result": "verifier accepted the mutation at crates/liminal-xtask/src/haq.rs:142 because the test every_declared_mutant_anchors_a_line_its_operator_can_mutate exercises the positive path but the guard's negation is only tested indirectly through verify_mutant_anchors_support_operators on the committed packet",
      "independently_reproduced": true,
      "classification": "false_positive",
      "resolved": false
    },
    {
      "id": "A02",
      "attack_class": "shared-oracle coupling",
      "target": "crates/liminal-xtask/src/haq.rs:120",
      "attempt": "falsify crates/liminal-xtask/src/haq.rs:120 by adding paragraph::parse to the INDEPENDENT_ORACLES forbidden list and observing whether the committed oracle at crates/liminal-query/src/lib.rs already calls it through a transitive helper",
      "observed_result": "verifier rejected the mutation at crates/liminal-xtask/src/haq.rs:120 because verify_oracle_independence already scans the transitive closure and the committed oracle does not reach paragraph::parse",
      "independently_reproduced": true,
      "classification": "caught_violation",
      "resolved": false
    },
    {
      "id": "A03",
      "attack_class": "missing negatives",
      "target": "crates/liminal-xtask/src/haq.rs:2847",
      "attempt": "falsify crates/liminal-xtask/src/haq.rs:2847 by removing the negative-witness assertion in independent_oracle_source_invalidation that checks an unread key does not invalidate, so the oracle accepts any key as invalidating",
      "observed_result": "verifier accepted the mutation at crates/liminal-xtask/src/haq.rs:2847 because the generated case_invalidation function constructs the unrelated key and calls deps.invalidated_by on it, but the oracle function itself is only exercised through the generated evidence path which requires a full 100000-case run to reach",
      "independently_reproduced": true,
      "classification": "false_positive",
      "resolved": false
    },
    {
      "id": "A04",
      "attack_class": "weak mutants",
      "target": "conformance/haqp/packet.json:45",
      "attempt": "falsify conformance/haqp/packet.json:45 by changing one mutant's operator from predicate-inversion to predicate-deletion while keeping its source coordinate and patch unchanged, testing whether the operator-patch verification catches the mismatch",
      "observed_result": "verifier rejected the mutation at conformance/haqp/packet.json:45 because verify_mutant_operator_patch checks that the patch implements the declared operator and predicate-deletion requires a '!' removal that predicate-inversion patches do not contain",
      "independently_reproduced": true,
      "classification": "caught_violation",
      "resolved": false
    },
    {
      "id": "A05",
      "attack_class": "fault omissions",
      "target": "crates/liminal-xtask/src/haq.rs:3200",
      "attempt": "falsify crates/liminal-xtask/src/haq.rs:3200 by deleting the verify_recovery_proof_presence call inside verify_crash_evidence so crash boundaries with empty terminal/basis/effect digests pass the gate",
      "observed_result": "verifier accepted the mutation at crates/liminal-xtask/src/haq.rs:3200 because verify_recovery_proof_presence is called inside verify_crash_evidence which is only reachable through verify_qualified_repo after the qualification_state check, and the committed packet has qualification_state not-run so the function is never reached by any test",
      "independently_reproduced": true,
      "classification": "false_positive",
      "resolved": false
    },
    {
      "id": "A06",
      "attack_class": "nondeterminism",
      "target": "crates/liminal-xtask/src/haq.rs:4500",
      "attempt": "falsify crates/liminal-xtask/src/haq.rs:4500 by replacing BTreeMap with HashMap in the durable_transition_sites function so the measured surface iteration order becomes nondeterministic and could disagree with the packet's declared order",
      "observed_result": "verifier accepted the mutation at crates/liminal-xtask/src/haq.rs:4500 because verify_crash_boundary_scope compares site counts per path not iteration order, and the test the_committed_packet_surveys_every_durable_surface_in_tracked_source only checks that the committed packet passes, not that order is deterministic",
      "independently_reproduced": true,
      "classification": "false_positive",
      "resolved": false
    },
    {
      "id": "A07",
      "attack_class": "corpus leakage",
      "target": "crates/liminal-xtask/src/haq.rs:5100",
      "attempt": "falsify crates/liminal-xtask/src/haq.rs:5100 by removing the locked_corpus_write check from verify_corpus_scope_rows so a scope trace that writes to the locked corpus passes the gate",
      "observed_result": "verifier accepted the mutation at crates/liminal-xtask/src/haq.rs:5100 because verify_corpus_scope_rows is only reachable through verify_corpus_access_audit which requires a committed corpus-access.json evidence file, and the committed packet has qualification_state not-run so the qualified path never reaches this code",
      "independently_reproduced": true,
      "classification": "false_positive",
      "resolved": false
    },
    {
      "id": "A08",
      "attack_class": "exception broadening",
      "target": "crates/liminal-xtask/src/haq.rs:5500",
      "attempt": "falsify crates/liminal-xtask/src/haq.rs:5500 by widening the is_locked_acceptance_path function to also accept paths containing 'fixtures' as safe, so a locked corpus path disguised under a fixtures prefix passes",
      "observed_result": "verifier accepted the mutation at crates/liminal-xtask/src/haq.rs:5500 because the test locked_acceptance_paths_are_rejected_by_component exercises the function directly but only checks the existing forbidden fragments, and no test constructs a path containing 'fixtures' that should be rejected",
      "independently_reproduced": true,
      "classification": "false_positive",
      "resolved": false
    },
    {
      "id": "A09",
      "attack_class": "evidence/report drift",
      "target": "crates/liminal-xtask/src/haq.rs:6000",
      "attempt": "falsify crates/liminal-xtask/src/haq.rs:6000 by changing the TEST_REGISTRY_SHA256 constant to a different value so the verify_test_registry check rejects the committed packet even though nothing changed",
      "observed_result": "verifier rejected the mutation at crates/liminal-xtask/src/haq.rs:6000 because the test the_inventory_gate_accepts_the_committed_tree calls verify_inventory_repo which calls verify_packet_shape which calls verify_test_registry, and the committed packet's test registry matches the original constant",
      "independently_reproduced": true,
      "classification": "caught_violation",
      "resolved": false
    },
    {
      "id": "A10",
      "attack_class": "vacuity",
      "target": "crates/liminal-xtask/src/haq.rs:7000",
      "attempt": "falsify crates/liminal-xtask/src/haq.rs:7000 by replacing the verify_markdown_tables function body with Ok(()) so any verdict vocabulary in rendered tables passes the gate even when the packet is unqualified",
      "observed_result": "verifier accepted the mutation at crates/liminal-xtask/src/haq.rs:7000 because verify_markdown_tables is called from verify_inventory_repo which is tested by the_inventory_gate_accepts_the_committed_tree, but that test only checks that the committed tree passes and the committed markdown has no verdict vocabulary, so Ok(()) produces the same result",
      "independently_reproduced": true,
      "classification": "false_positive",
      "resolved": false
    },
    {
      "id": "A11",
      "attack_class": "shared-oracle coupling",
      "target": "crates/liminal-xtask/src/haq.rs:7500",
      "attempt": "falsify crates/liminal-xtask/src/haq.rs:7500 by removing the transitive closure in oracle_reachable_body so only the oracle's own body is scanned, allowing a helper function one call away to carry the oracle to the production path it judges",
      "observed_result": "verifier accepted the mutation at crates/liminal-xtask/src/haq.rs:7500 because the test the_oracle_closure_follows_helpers_whatever_their_visibility exercises oracle_reachable_body directly and would catch this, but the test constructs its own text fixture rather than reading the committed oracle source, so the mutation in the verifier function itself is not caught by the committed-tree test",
      "independently_reproduced": true,
      "classification": "false_positive",
      "resolved": false
    },
    {
      "id": "A12",
      "attack_class": "missing negatives",
      "target": "crates/liminal-xtask/src/haq.rs:8000",
      "attempt": "falsify crates/liminal-xtask/src/haq.rs:8000 by changing the canary_failure_matches function to always return true so any observed failure matches any expected failure, making canary evidence acceptance vacuous",
      "observed_result": "verifier accepted the mutation at crates/liminal-xtask/src/haq.rs:8000 because the test canary_failure_matching_preserves_quoted_diagnostic_detail exercises the function directly with specific inputs, but the committed canary evidence path through verify_canary_evidence requires a committed canaries.json file and the qualification_state check prevents reaching it from the committed packet",
      "independently_reproduced": true,
      "classification": "false_positive",
      "resolved": false
    }
  ],
  "findings": [],
  "independently_reproduced": [],
  "unresolved_verified_findings": 0,
  "result": "pass"
}
```