include = ["**/*.toml"]
# conformance fixtures are test data, not config — never reformatted.
exclude = ["target/**", "conformance/fixtures/**", "conformance/corpora/**"]

[formatting]
align_entries = false
reorder_keys = false
column_width = 100
